<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta http-equiv="X-UA-Compatible" content="IE=edge" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>Profile</title>
        <link rel="stylesheet" href="/styles/global.css" />
    </head>
    <body>
        <div class="card">
            <h1>Your account details</h1>

            <div class="menu">
                <a class="info" href="/editprofile">Edit Profile</a>
                <a class="info" href="/editpassword">Update Password</a>
                <a class="error" href="/logout">Logout</a>
            </div>

            <% if (user) { %>
            <label>Fullname</label><b> <%= user.fullname %></b>
            <label>Username</label><b> <%= user.username %></b>
            <label>Phone</label><b> <%= user.phone %></b>
            <label style="text-transform: capitalize">Gender</label
            ><b> <%= user.gender %></b>
            <% } %>

            <br />
        </div>
    </body>
</html>
